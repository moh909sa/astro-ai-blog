---
import BlogCard from '@/components/BlogCard.astro'
import Link from '@/components/Link.astro'
import PageHead from '@/components/PageHead.astro'
import { buttonVariants } from '@/components/ui/button'
import { SITE } from '@/consts'
import Layout from '@/layouts/Layout.astro'
import { getRecentPosts } from '@/lib/data-utils'

const blog = await getRecentPosts(SITE.featuredPostCount)

// Safe fallback headline
const headline = "🚀 Discover the Smartest Tools on the Internet"
---

<Layout class="max-w-3xl">
  <PageHead slot="head" title="Home" />

  <section>
    <div class="rounded-lg border bg-background">
      <div class="flex flex-col space-y-1.5 p-6">
        <h1 class="text-4xl font-extrabold text-foreground">
          {headline}
        </h1>
        <p class="text-muted-foreground text-base">
          Welcome to your new favorite resource for AI tools, affiliate strategies, and digital growth.
        </p>
      </div>
      <div class="p-6 pt-0 text-muted-foreground text-sm space-y-2">
        <p>
          We share powerful tools and automation tips to help you build faster, grow smarter, and succeed online.
        </p>
        <p>
          Whether you're into affiliate marketing, YouTube automation, or digital tools — you're in the right place.
        </p>
        <p>
          🧠 Learn with us. Grow with us.
        </p>
      </div>
    </div>
  </section>

  <section class="flex flex-col gap-y-4 mt-10">
    <h2 class="text-2xl font-medium">📝 Latest Articles</h2>
    <ul class="flex flex-col gap-y-4">
      {
        blog.map((post) => (
          <li>
            <BlogCard entry={post} />
          </li>
        ))
      }
    </ul>
    <div class="flex justify-center mt-4">
      <Link
        href="/blog"
        class={buttonVariants({ variant: 'ghost' }) + ' group'}
      >
        See all posts
        <span class="ml-1.5 transition-transform group-hover:translate-x-1">
          &rarr;
        </span>
      </Link>
    </div>
  </section>
</Layout>
